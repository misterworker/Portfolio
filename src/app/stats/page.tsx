import StatsOverview from '@/components/stats/StatsOverview';
import Header from '@/components/Header'

export default function StatsPage() {
  const username = 'misterworker';
  const repo = 'Portfolio';

  return (
    <>
      <Header/>
        {/* <div className="p-8">
          <h1 className="text-3xl font-semibold text-gray-100 mb-6 text">GitHub Statistics for {username}/{repo}</h1>
          <StatsOverview username={username} repo={repo} />
        </div> */}
        // TODO: Revive
    </>
  );
}
